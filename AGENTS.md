# AGENTS.md

本文件定义 AI 代理在本仓库中工作时必须遵守的规则。任何 AI 在开始工作前必须先阅读并严格执行本文件。

## 1. 禁止任何形式的 AI 进行任何形式的 Git "写操作"

**这是最高优先级的红线规则，没有任何例外，也不接受任何"顺手帮忙"式的变通。**

AI 禁止执行一切会改变本地仓库状态、暂存区、提交历史或远程仓库的 Git 操作，包括但不限于：

- `git add` / `git rm` / `git mv`（暂存与删除跟踪）
- `git commit`（含 `--amend`）
- `git push`（含 `--force` / `-f`）
- `git merge` / `git rebase` / `git cherry-pick` / `git revert`
- `git reset` / `git restore --staged` / `git checkout <分支>` / `git switch`
- `git branch` 的创建与删除、`git tag` 的创建与删除、`git stash`
- 通过脚本、子进程、MCP 工具或其他任何间接方式完成的上述等价操作

AI **只允许**执行只读 Git 命令，例如：`git status`、`git log`、`git diff`、`git show`、`git branch -l`、`git remote -v`、`git ls-remote`。

所有提交、推送、分支管理等写操作一律由用户本人独立管控。AI 可以按任务要求修改普通工作区文件，但绝不能代为执行任何 Git 写操作。

除非用户明确要求提供 Git 操作参考，否则 AI 不得主动：

- 规划提交、暂存、分支、标签、合并、变基、推送或拉取请求流程；
- 建议用户执行任何 Git 写命令；
- 生成提交信息、分支名或 Git 写命令；
- 在任务总结中提醒用户暂存、提交或推送；
- 把“便于提交”“保持 Git 状态整洁”或类似理由作为文件修改决策依据。

如果用户明确要求提供 Git 操作参考，AI 只能输出供用户自行判断和执行的纯文本，不得通过任何工具、脚本或间接方式执行。

### 1.1 禁止基于 Git 管控推测用户意图

Git 的跟踪状态、忽略规则、暂存状态、分支状态和远程状态，只代表用户对版本管理的实际管控，不构成对任务范围、文件路径或交付内容的补充指令。

AI 必须遵守：

- 用户明确指定的文件或目录路径具有最高执行优先级；
- 即使目标路径被 `.gitignore` 忽略，AI 也必须按用户指定路径创建或修改内容；
- 不得因为文件未被 Git 跟踪、被忽略、位于仓库外或当前分支状态特殊，就擅自更改执行路径；
- 不得为了让文件进入版本管理而自动修改 `.gitignore`、`.git/info/exclude` 或其他 Git 配置；
- 不得因为推测文件“不会被提交”，就跳过、缩减、替换或省略用户要求的交付内容；
- 不得把用户要求的文件移动到一个“更适合 Git 管理”的位置；
- 不得依据 `git status`、`git diff` 等只读结果，擅自扩大或缩小任务范围；
- 如果 Git 状态与用户明确要求看似冲突，仍应完成用户明确要求；只有文件系统安全、权限或内容覆盖风险无法合理消除时，才向用户说明实际阻碍。

### 1.2 Git 只读检查的使用边界

AI 只应在确有必要时使用只读 Git 命令，例如核对现有改动、避免覆盖用户工作或按用户要求查看历史。只读检查不得成为修改项目配置、改变交付路径或缺省任务内容的理由。
